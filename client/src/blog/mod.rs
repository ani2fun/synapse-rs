//! The blog feature (oracle: `client/blog/`, step 19) — the listing page and the post reader
//! over the shared blog DTOs. Prose only: the post body rides the same markdown island as
//! lessons but hydrates no workbenches.

pub mod state;
pub mod view;
